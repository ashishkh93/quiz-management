const ROOTS = {
  DASHBOARD: "/dashboard",
};

// ----------------------------------------------------------------------

export const paths = {
  // AUTH
  auth: {
    login: "/login",
  },
  dashboard: {
    root: "/quiz-dashboard",
  },
};
